#pragma once

#include <vector>

namespace stage_01::references_const::increment_all_exercise {

void increment_all(std::vector<int>& values);

}  // namespace stage_01::references_const::increment_all_exercise
