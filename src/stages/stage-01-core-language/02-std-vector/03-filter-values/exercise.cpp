#include "exercise.hpp"

#include <stdexcept>

namespace stage_01::std_vector::filter_values_exercise {

// Task: Return all values that are greater than or equal to minimum.
// Preserve the original relative order.
std::vector<int> filter_values(const std::vector<int>& values, int minimum) {
    (void)values;
    (void)minimum;
    throw std::logic_error("TODO: implement filter_values");
}

}  // namespace stage_01::std_vector::filter_values_exercise
