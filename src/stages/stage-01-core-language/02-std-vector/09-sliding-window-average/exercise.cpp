#include "exercise.hpp"

#include <stdexcept>

namespace stage_01::std_vector::sliding_window_average_exercise {

// Task: Return the average of each contiguous window of window_size values.
// Return an empty vector when window_size is 0 or larger than values.size().
std::vector<double> sliding_window_average(const std::vector<double>& values, std::size_t window_size) {
    (void)values;
    (void)window_size;
    throw std::logic_error("TODO: implement sliding_window_average");
}

}  // namespace stage_01::std_vector::sliding_window_average_exercise
