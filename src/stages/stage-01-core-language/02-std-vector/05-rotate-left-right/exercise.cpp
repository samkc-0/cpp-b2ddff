#include "exercise.hpp"

#include <stdexcept>

namespace stage_01::std_vector::rotate_left_right_exercise {

// Task: Return values rotated left by amount positions.
// Amounts larger than the vector size should wrap around.
std::vector<int> rotate_left(const std::vector<int>& values, std::size_t amount) {
    (void)values;
    (void)amount;
    throw std::logic_error("TODO: implement rotate_left");
}

// Task: Return values rotated right by amount positions.
// Amounts larger than the vector size should wrap around.
std::vector<int> rotate_right(const std::vector<int>& values, std::size_t amount) {
    (void)values;
    (void)amount;
    throw std::logic_error("TODO: implement rotate_right");
}

}  // namespace stage_01::std_vector::rotate_left_right_exercise
